{
	"$schema": "https://www.raycast.com/schemas/extension.json",
	"name": "toolbox-gitlab",
	"title": "GitLab",
	"description": "Merge requests of your GitLab projects: projects in the order you last opened them, filters by people, state, draft and pipeline, a detail panel, and titled links on the clipboard.",
	"icon": "icon.png",
	"author": "pavel_smirnov",
	"platforms": [
		"macOS"
	],
	"categories": [
		"Developer Tools"
	],
	"license": "MIT",
	"commands": [
		{
			"name": "show-mrs",
			"title": "GitLab: Show MRs",
			"description": "Pick a project, then browse and filter its merge requests.",
			"mode": "view"
		}
	],
	"preferences": [
		{
			"name": "claudeUsername",
			"title": "Claude's GitLab Username",
			"description": "Whose merge requests By Claude (⌘2) shows.",
			"type": "textfield",
			"required": false,
			"default": "claude",
			"placeholder": "claude"
		}
	],
	"dependencies": {
		"@raycast/api": "1.104.25",
		"@raycast/utils": "1.19.1",
		"yaml": "^2"
	},
	"devDependencies": {
		"@types/bun": "^1.4.2",
		"@types/node": "22.13.10",
		"@types/react": "19.0.10",
		"typescript": "^5"
	},
	"scripts": {
		"dev": "ray develop",
		"build": "ray build -e dist",
		"test": "bun test",
		"typecheck": "tsc --noEmit"
	}
}
